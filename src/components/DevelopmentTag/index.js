import React from 'react';

import styles from './styles.module.css';

const Development = () => (
  <div className={ styles.developmentTagContainer }>
    <p className={ styles.developmentTag }>Em desenvolvimento</p>
  </div>
);

export default Development;
